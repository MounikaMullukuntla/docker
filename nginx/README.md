# NGINX

nginx (pronounced "engine x") is a web server that can also be used as a reverse proxy, load balancer, mail proxy and HTTP cache.

You can use it to host multiple stacks in the same root by assigning ports to subfolders with Python (Flask), Rust, NodeJS, DotNet.

[Learn more](https://en.wikipedia.org/wiki/Nginx) - wikipedia